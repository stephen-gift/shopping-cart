'use client'
import { SCCarousel } from "@/components";
import SCCustomCarousel from "@/components/global/Carousel";
import React from "react";

type Props = {};

const HeroBannerContainer = (props: Props) => {
  return (
    <>
      <SCCustomCarousel />
    </>
  );
};

export default HeroBannerContainer;
