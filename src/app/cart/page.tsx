import { SCCartPageContainer } from "@/containers";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <>
      <SCCartPageContainer />
    </>
  );
};

export default page;
