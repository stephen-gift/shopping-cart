import { SCCheckoutContainer } from "@/containers";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <>
      <SCCheckoutContainer />
    </>
  );
};

export default page;
